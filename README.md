# log processor
 BMS log processor

To run:
- place raw bms log files in a folder
- run the script and select the folder containing the .csv files
- it will place each file in the selected folder in an identically named subfolder with processed data and plots along with the original file.
